# Docker

Los Dockerfiles permanecen junto a `backend/` y `frontend/` para conservar el contexto de build mínimo. Este directorio se reserva para configuraciones compartidas futuras, por ejemplo reverse proxy, certificados locales o herramientas de observabilidad.
